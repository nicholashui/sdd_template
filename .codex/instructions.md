<!-- AUTO-GENERATED by sdd_template. Do not edit directly.
Source: rules/, skills/, hooks/, mcp-configs/, knowledge/
Run: npm run sync
-->

# sdd_template — Codex Instructions

Codex also reads the root `AGENTS.md`. This file mirrors the shared rules.

<!-- BEGIN MANAGED AGENT RULES -->
# Constitution

- Follow the user's goal exactly.
- Prefer simple, maintainable solutions.
- Make surgical changes.
- Do not perform destructive actions without approval.
- Do not install, execute, or import downloaded third-party code until audited.
- Run relevant tests or explain why tests were not run.
- Update status after major work.

---

# Karpathy-Style Agent Rules

- Think before coding.
- Do not assume hidden requirements.
- Prefer the simplest working solution.
- Avoid speculative abstractions.
- Change only what is necessary.
- Keep the goal visible.
- Verify the result.

---

# Spec-Driven Development (SDD)

- Start from a clear specification before writing code.
- Capture requirements, design, and tasks explicitly.
- Keep the spec and the implementation in sync; update the spec when scope changes.
- Break work into small, verifiable tasks and complete them in order.
- Treat acceptance criteria as the definition of done.
- Reference the source spec (`project_starter.md`) when in doubt.

---

# Security

- Downloaded upstream repositories are untrusted until audited.
- Never execute scripts from downloaded sources.
- Never run `npm install`, `curl | bash`, or remote installers from a source repo.
- Never copy third-party code into active agent config without curation and approval.
- Keep secrets out of the repository (`.env`, keys, tokens, certificates).
- Restrict MCP servers to the minimum filesystem and network access required.
- Record provenance (URL, commit, license) for any curated import.

---

# Testing

- Run relevant tests after meaningful changes.
- Prefer fast, deterministic tests that do not require network access.
- Use the Node built-in test runner (`node --test`).
- Validate manifests, generated output headers, and lockfile shape.
- If tests cannot be run, explain why explicitly.
- Do not weaken or delete tests to make a change pass.

---

# Token Efficiency

- Keep context focused; load only what is needed for the task.
- Prefer concise, structured outputs over verbose narration.
- Reuse curated rules and skills instead of re-deriving them each run.
- Summarize long sources rather than embedding them wholesale.
- Avoid redundant re-reads of files already in context.

---

# Human Approval

Human approval is required before:

- installing global packages,
- running remote install scripts,
- enabling MCP servers with credentials,
- copying third-party repo code into active agent configs,
- modifying hooks,
- deleting files,
- applying self-generated skill/rule changes.

---

# Knowledge Base (shared across all coding agents)

This project vendors a curated, audited knowledge corpus under `knowledge/` so every supported coding agent works from the same playbook. The files are plain Markdown — readable and usable by any agent operating in this repo.

- **Skills** (262) — `knowledge/skills/<name>/SKILL.md`
- **Agents** (64) — `knowledge/agents/<name>.md`
- **Commands** (84) — `knowledge/commands/<name>.md`
- **Full catalog** — `knowledge/INDEX.md`
- **Provenance & license** — `knowledge/NOTICE.md` (source: ecc (https://github.com/affaan-m/ECC), MIT)

## How to use this knowledge

1. When a task matches a skill's trigger (see its `description`), open that skill's `SKILL.md` and follow it.
2. For domain work, adopt the most relevant **agent** persona from `knowledge/agents/` (e.g. `security-reviewer`, `code-reviewer`, `architect`).
3. Treat **commands** in `knowledge/commands/` as repeatable playbooks for common workflows (review, build-fix, planning, testing).
4. Browse `knowledge/INDEX.md` for the complete, categorized list. This corpus is reference guidance, not auto-executed code.
<!-- END MANAGED AGENT RULES -->

