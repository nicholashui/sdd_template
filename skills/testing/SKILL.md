# Skill: Testing

Author deterministic tests using the Node built-in runner.

## Guidance
- Use `node:test` and `node:assert/strict`.
- Avoid network in unit tests; test pure functions where possible.
- Validate manifests, generated headers, and lockfile shape.
- Run `npm run test` before declaring work done.
