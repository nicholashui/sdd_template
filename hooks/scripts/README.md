# hooks/scripts/

First-party hook scripts referenced by `hooks/manifest.json`.

- Hooks are opt-in and must be listed in the manifest to be considered active.
- Hooks copied or adapted from downloaded sources require human approval before use.
- Hooks must never execute downloaded third-party code directly.
