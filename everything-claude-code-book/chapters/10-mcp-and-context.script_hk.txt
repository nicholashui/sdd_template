第十章。MCP 同上下文管理。

我哋由 MCP 係乜開始講。MCP，即係 Model Context Protocol，係一個標準嘅方式，將你嘅助手連接到外部服務：GitHub、一個資料庫、一個部署平台、一個瀏覽器。一個 MCP 伺服器暴露出模型可以直接呼叫嘅工具。唔使你將一個資料庫結果複製貼上去對話，模型自己會查詢個資料庫。短篇指南講得好好：一個 MCP 係一個由提示驅動、包住一個 API 嘅包裝。佢唔係 API 嘅替代品，而係模型導航佢嘅一個靈活方式。例如，一個資料庫 MCP 等助手可以列出資料表並且執行查詢，唔使離開個會話。

ECC 將佢嘅 MCP 設定放喺一個檔案，並且剛好內附一個預設連接器，畀 Chrome 開發者工具用，提供瀏覽器控制。其餘一切都係可選擇加入，要麼係一個包住命令列工具或者 REST API 嘅 skill，要麼就係一個你刻意啟用嘅目錄項目。呢個係一個有記錄嘅政策，佢嘅存在係為咗保護呢一章真正要講嘅嘢：你嘅上下文視窗。

呢度係成個 ECC 入面最重要嘅操作教訓，佢令差唔多每個人都意外，所以讀兩次。每一個 MCP 工具描述，喺你開始做任何嘢之前，就已經喺度消耗你上下文視窗嘅 token。太多啟用咗嘅 MCP，可以將一個二十萬 token 嘅視窗縮到大約七萬，而當你接近上限，品質會劇烈下降。每一個啟用咗嘅 MCP 伺服器都會宣傳佢嘅工具，而每一個工具嘅描述都永久咁坐喺上下文裏面。啟用十幾個多嘴嘅 MCP 伺服器，你就花咗三分一嘅視窗喺啲你可能永遠都唔會用嘅選單上面。

所以呢度係經驗法則，直接由指南嚟。保持啟用少過十個 MCP 伺服器。保持少過八十個工具活躍。設定咗二三十個 MCP 完全冇問題。淨係逐個專案停用你冇用嗰啲就得。你可以用 MCP command 睇吓啟用咗啲乜，並且喺嗰度停用冇用嗰啲。一個提示：MCP command 係可靠嘅即時開關。編輯你嘅設定檔，唔係一個可靠嘅方法去關閉已經載入嘅 MCP 伺服器，所以用 MCP command。

長篇指南分享咗一個犀利嘅優化：用命令列工具代替 MCP 嘅技巧。好多 MCP 係可以替代嘅。好似 GitHub 嘅版本控制、資料庫，同部署平台，已經有出色嘅命令列工具，而 MCP 本質上就係喺度包住佢哋。所以唔好成日載入一個 MCP，而係建立一個包住你需要嗰個命令列工具嘅 skill 或者 command。例如，一個用你偏好嘅旗標去執行 GitHub CLI 嘅拉取請求 command。你攞到方便，又冇咗嗰個成日存在嘅上下文成本。呢個係任何有好命令列工具嘅嘢嘅推薦預設。將 MCP 留畀啲工具呼叫人體工學真係有幫助嘅服務。

你喺 ECC 入面點樣設定 MCP 呢？因為外掛安裝唔自動啟用內附嘅 MCP，所以你要選擇加入。你可以喺 Claude Code 入面用 MCP command 即時做，佢會加入、啟用或者停用伺服器並且留存個改動。又或者，為咗 repo 本地存取，你將你想要嘅伺服器複製入一個專案設定檔，並且將任何佔位金鑰換成真正嘅。而如果你已經行緊自己嗰份 ECC 內附嘅 MCP，你就設定一個列出佢哋嘅環境變數，咁安裝同同步流程就會跳過佢哋，而唔係再加重複嘅。嗰個環境變數係一個安裝同同步嘅過濾器，唔係一個即時開關。ECC 知道嘅嗰套 MCP 伺服器，即係你逐個專案淨係會啟用一手指頭數得晒嗰啲，包括 GitHub、一個資料庫連接器、一個實時文件服務、一個神經搜尋服務、記憶、一個瀏覽器自動化工具、一個依次序思考嘅助手、幾個部署平台、一個分析資料庫、一個網絡爬取服務，同預設嘅 Chrome 開發者工具連接器。

不過上下文紀律唔淨止關於 MCP。ECC 同啲指南推薦咗幾個其他習慣。第一，用一個語意搜尋工具，代替純文字搜尋。喺 ECC 嘅五十項任務基準測試裏面，語意搜尋喺相似或者更好嘅品質下面，用咗大約一半嘅 token。第二，保持程式碼庫模組化，檔案係數百行而唔係數千行。細啲嘅檔案意味住更平嘅讀取同更好嘅一擊即中，而呢個亦都係一條 rule。第三，用策略性壓縮：停用自動壓縮，改為喺合乎邏輯嘅間隔壓縮。ECC 有一個喺好時機推你一把嘅 hook，而壓縮前 hook 會先儲存狀態。第四，倚靠上下文監察，即係嗰個喺上下文耗盡、失控成本、範圍蔓延同工具回圈咬到你之前警告你嘅 hook。而第五，用 context-budget command 去分析你嘅 token 去咗邊，並且削減開銷。會話開始嘅上下文注入本身就有封頂，喺一個合理嘅預設值，而你可以調校佢，又或者為低上下文嘅設置完全關閉佢。

有一個 MCP 嘅近親值得一提：外掛。外掛打包工具，方便安裝，而一個單一嘅外掛可以捆綁 skills、hooks、MCP 伺服器，或者語言伺服器整合。同樣嘅上下文警告適用。啟用好多外掛，連同佢哋嘅工具，會食你嘅視窗。作者保持大約十四個外掛裝住，但一次淨係啟用四五個。特別有用嘅外掛類型，包括語言伺服器外掛，喺你喺 IDE 以外運行助手嗰陣，畀你實時型別檢查同跳去定義；hookify 外掛，用嚟用對話方式建立 hooks；語意搜尋外掛；同一個實時文件外掛。

咁，重點精華。MCP 將模型連接到外部服務，做可呼叫嘅工具，而 ECC 預設淨係內附 Chrome 開發者工具連接器。每一個啟用咗嘅 MCP 都會預先花費上下文 token，所以保持啟用少過十個伺服器同少過八十個工具：設定多啲，啟用少啲。用 MCP command 做即時開關，唔係用設定檔。對於有好命令列工具嘅服務，偏向用一個包住命令列工具嘅 skill，多過一個成日存在嘅 MCP。並且用語意搜尋、模組化檔案、策略性壓縮、上下文監察，同 context-budget command 嚟保護你嘅上下文。

而家我哋將以上一切，砌成你實際上每日會運行嘅工作流程。
