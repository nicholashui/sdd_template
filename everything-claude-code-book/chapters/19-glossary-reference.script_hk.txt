第十九章。詞彙表同快速參考。

呢個係一頁式嘅速查表，係值得收藏嘅一章。我哋過一過啲關鍵術語。

ECC 即係 Everything Claude Code，本書中心嗰套代理框架操作系統。佢唔係橢圓曲線密碼學。一個框架係包住一個模型、令佢可以行動嘅程式，例如 Claude Code、Cursor、Codex，或者 OpenCode。一個 agent，又或者 sub-agent，係 orchestrator 委派工作畀佢嘅、有明確範圍嘅專家，有佢自己嘅上下文、有限嘅工具，同一個揀好嘅模型。Orchestrator 係委派工作畀 agents 並且匯集佢哋結果嘅主助手。一個 skill 係標準嘅可重用工作流程單位。一個 command 係一個打出嚟嘅斜線項目，而佢係 skills 之上嘅一個相容層。一個 hook 係由一個工具或者會話事件觸發嘅自動化，將一個設定檔對應到細細嘅腳本。一條 rule 係一個每一次會話都會載入、要永遠遵守嘅指引，有通用嘅亦有各語言嘅。MCP，即係 Model Context Protocol，係一個暴露出模型可以呼叫嘅工具嘅伺服器，例如 GitHub、一個資料庫，或者一個瀏覽器。一個 instinct 係第二代學習系統裏面，一小塊有信心評分嘅學到行為單位。一個 codemap 係一份程式碼庫嘅輕量地圖，等助手唔使重新探索咁去導航。一個 profile 係一個安裝套裝，可以係 minimal、core，或者 full。一個 target 係一個安裝係畀邊款框架。代理資料家目錄係 ECC 嘅會話、記憶同度量資料嘅根目錄。致命三重奏係私人資料加不可信內容加對外通訊喺一個運行時裏面，等於外洩風險。AgentShield 係 ECC 內附嘅安全審計器。提示防禦基線係出現喺每一個 agent 同主指令檔案裏面嗰個反注入嘅 rule 區塊。ECC 2.0 係管理好多會話嘅 Rust 控制平面原型，仍然喺 alpha。而 pass-at-k 對 pass-to-the-k，係評估可靠性嘅度量，分別意味住 k 次嘗試之中至少一次成功，對比 k 次嘗試全部成功。

記住 ECC 嘅三個名。GitHub repo 用一個名。外掛同 marketplace 用一個簡短嘅識別碼。而 npm 套件用第三個名。佢哋全部指嘅都係同一個專案。

呢度係安裝嘅快速參考，用淺白嘅話講。至於外掛，喺 Claude Code 入面，你加入 marketplace 並且安裝外掛。至於手動路線，你 clone 個 repo、安裝相依套件，並且用一個設定檔同目標執行安裝器；minimal 意味住冇 hooks，你可以保留 core 嘅同時停用 hooks，你可以遲啲用 hooks-runtime 模組加 hooks，而你可以用 npx 安裝器、唔使 clone 就安裝。要搵裝啲乜，用 consult command。至於 rules，佢哋永遠係手動嘅，整一個 rules 目錄並且複製 common 嘅 rules 加你技術棧嘅語言包。至於生命週期同重設，用 list-installed、doctor 同 repair，跟住試行解除安裝，然後真正解除安裝。而至於其他框架，你將目標設做 Cursor、為 Codex 執行同步腳本、為 Zed 用 minimal 設定檔，又或者淨係喺 repo 入面啟動 OpenCode。黃金法則，再講一次：揀一條路線。永遠唔好外掛同完整安裝器一齊用。

啲 commands 亦都按工作分組。有一個規劃同建構嘅組、一個測試嘅組、一個審查嘅組、一個建構修復嘅組、一個驗證嘅組、一個會話嘅組、一個學習嘅組、一個文件嘅組、一個回圈嘅組、一個框架嘅組，同一個拉取請求嘅組。外掛形式會喺 commands 前面加一個 ecc 命名空間。

仲有一份值得知嘅環境變數參考。Hook 設定檔變數將嚴格度設做 minimal、standard，或者 strict。停用 hooks 嘅變數，接受一個以逗號分隔、要關閉嘅 hook id 清單。有變數去封頂或者停用會話開始嘅上下文、去設定會話保留視窗，同去隱藏成本估算。代理資料家目錄變數設定會話、記憶同度量資料嘅根目錄，呢個就係你隔離各框架嘅方法。停用 MCP 嘅變數係一個安裝同同步嘅過濾器，去跳過內附嘅伺服器。有一個變數去啟用管治事件捕捉、一個畀第二代學習系統存放佢嘅 instincts、一個去強制一個特定嘅套件管理器，同 base-URL 同 auth-token 變數畀一個自訂閘道，喺嗰度你應該淨係信任你自己嗰個。

至於 token 優化嘅預設，喺你嘅設定，用平衡嘅模型、將最大思考 token 設做一萬，並且將自動壓縮設做五成。按任務路由模型：搜尋同簡單工作用最平嘅、平衡嘅做編程嘅預設，而最強大嘅就畀架構、安全，同困難嘅除錯。保持少過十個 MCP 同少過八十個工具。用語意搜尋。並且保持檔案模組化。

至於安全嘅最低門檻：用一個配有範圍 tokens 嘅獨立身份；喺一個冇網絡嘅容器或者虛擬機裏面隔離不可信嘅工作；為你嘅 SSH 金鑰、雲端憑證、環境檔案，同危險嘅 shell 指令設定拒絕 rules；用最小權限逐個工作流程限定工具；淨化輸入並且保留提示防禦基線；要求批准，配記錄同一個緊急停止掣；運行 AgentShield 掃描並且修咗啲關鍵問題；同唔好喺你嘅主機上面 clone 同打開不可信嘅 repos。

而呢度係用一幅圖講嘅心智模型。喺最頂係身份同管治，即係憲法。佢管治組件目錄，由 agents、skills、commands、rules 同 MCP 組成。嗰個目錄由轉接器同安裝器塑造，配佢哋嘅清單、狀態儲存，同目標旗標。而嗰樣嘢由運行時落地同強制執行，運行時由 hooks 同腳本、記憶、學習、度量，同 ECC 2.0 控制平面組成。五個原則橫切所有層：代理優先、測試驅動、安全優先、不可變性，同先規劃後執行。

你下一步應該去邊？讀 repo 自己嘅指南，由短篇指南開始，然後長篇指南，然後安全指南。睇 command 快速參考，攞實時嘅 command 清單。並且探索 docs 資料夾，深入了解架構、發佈、各框架指南，同國際化。

而講到呢度，你就去到結尾。你而家知道 ECC 係乜、佢點解存在、佢點樣建構、點樣乾淨咁安裝佢、點樣跨框架日常操作佢、點樣令佢學習、點樣令佢保持快速，同點樣令佢保持安全。去建造啦，並且等啲 skills 產生複利。
