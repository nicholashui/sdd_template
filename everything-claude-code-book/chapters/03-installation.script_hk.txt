第三章。安裝同設定。

呢一章要做到完全正確，因為一個馬虎嘅安裝，係 ECC 畀人感覺壞咗或者重複咗嘅頭號原因。好消息係，啲規則好簡單。

我哋由一條可以避免大部分問題嘅規則開始講。揀返剛好一條安裝路線，永遠唔好將佢哋疊埋一齊。有兩條路線。第一條係外掛，係 Claude Code 推薦嘅預設做法。第二條係手動安裝器，又或者係命令列介面。最經典嘅壞設定，就係執行咗外掛安裝，跟住又再執行完整安裝器。外掛已經載入咗 ECC 嘅 skills、commands 同 hooks。完整安裝器跟住又將同樣嘅嘢複製入你嘅用戶目錄，搞到你有重複嘅 skills 同重複嘅運行時行為。如果你已經咁做咗，你第一站應該係本章稍後嘅重設部分，喺你做任何其他嘢之前。

而家講要求。你需要 Claude Code 嘅命令列工具，2.1.0 版或者之後嘅版本，因為外掛嘅 hook 自動載入要靠佢。你需要 Node，因為 hooks 同腳本係基於 Node，可以喺 Windows、macOS 同 Linux 跨平台運行。你亦都需要一個套件管理器，無論係 npm、pnpm、yarn 定 bun。ECC 會自動偵測你用緊邊個。

我哋行一行路線 A，即係外掛，係 Claude Code 推薦嘅做法。喺一個 Claude Code 會話入面，你加入指向 ECC repo 嘅 marketplace，然後安裝 ecc 呢個外掛。你亦都可以喺你嘅 Claude 設定檔裏面，用宣告式嘅方法接駁佢，喺嗰度列出 marketplace 並啟用外掛。咁就即刻畀到你 agents、skills、commands 同 hooks。但唔包括 rules，呢點我哋陣間會再講。

呢度有一個重要嘅 hook 陷阱，主要係畀貢獻者注意。唔好喺外掛嘅清單檔案裏面加一個 hooks 欄位。Claude Code 2.1 版同之後，會按照慣例自動載入一個外掛嘅 hooks 設定，如果你再宣告多次，就會引致一個 hooks 檔案重複嘅錯誤。甚至仲有一個迴歸測試喺度防住呢樣嘢，正正係因為佢之前一次又一次咁咬親個專案。而如果你覺得 hooks 太煩擾，又或者你淨係想要 rules、agents、commands 同核心 skills，你可以跳過外掛，改用 minimal 嘅手動設定檔。Hooks 係刻意唔包括喺 minimal 設定檔入面。

而家講路線 B，手動安裝器。你 clone 個 repo，安裝相依套件，然後用一個設定檔同一個目標去執行安裝器。例如，你可能會安裝 minimal 設定檔，目標係 Claude Code。喺 Windows 上面，有一個 PowerShell 版本，而你甚至可以唔使 clone，用 npx 安裝器嚟執行佢。

我哋講吓設定檔。Minimal 設定檔畀你 rules、agents、commands 同核心工作流程 skills，但冇 hooks 運行時。Core 設定檔畀你標準嗰套，而你可以保留 core 嘅同時，用一個旗標停用 hooks。Full 設定檔畀你所有嘢，而你應該淨係喺完全手動嘅路線上面用佢，永遠唔好喺外掛安裝之後用。

ECC 仲有一個由清單驅動嘅選擇性安裝器，所以你可以精準咁加入或者移除組件。你可以遲啲先加返淨係 hooks 運行時。你可以保留 core 但跳過 hooks。又或者你可以喺 minimal 之上，加入一個特定能力，例如機器學習。而如果你唔知要裝啲乜，問顧問啦。有一個 consult command，你描述一個需求，例如安全審查，又或者模型訓練同部署，佢就會交返相符嘅組件、相關嘅設定檔，同預覽同安裝嘅 commands。如果你想檢查確實嘅檔案計劃，你可以喺安裝之前先預覽。

呢度有樣嘢令人人都絆親：rules 永遠需要手動複製。Claude Code 外掛無法分發 rules。呢個係上游嘅限制。所以無論你揀咗邊條路線，如果你想要 ECC 嗰啲要永遠遵守嘅指引，你都要自己複製佢。你可以喺用戶層級安裝佢哋，套用喺所有專案，又或者喺專案層級安裝，淨係套用喺一個專案。幾條經驗法則。由 common 嘅 rules 加一個你實際會用嘅語言包開始。複製成個語言目錄，而唔係入面個別嘅檔案，咁相對引用同檔案名先唔會撞。而除非你真係想載入晒咁多上下文，否則唔好複製每一個 rules 目錄。

如果你行緊手動路線，正確咁安裝 hooks。唔好親手將 repo 嘅 hooks 設定複製入你嘅設定檔。嗰個檔案係面向外掛同 repo 嘅，唔支援原樣複製。改用安裝器啦，咁啲 command 路徑先會被正確咁改寫。安裝器將解析好嘅 hooks 寫入啱嘅位置，並且唔郁你嘅設定檔。而如果你用咗外掛，你已經自動載入咗 hooks，所以唔好再將佢哋複製入你嘅設定。

而家講重設同解除安裝。如果 ECC 畀人感覺重複、煩擾或者壞咗，唔好喺佢自己上面再裝多次。先診斷同清理。由生命週期包裝器開始。你可以列出裝咗啲乜、執行一個 doctor command 去診斷、執行一個 repair command 去還原 ECC 管理嘅檔案，同埋用試行模式執行解除安裝去預覽。仲有一個帶預覽選項嘅直接解除安裝腳本。ECC 淨係移除佢喺安裝狀態裏面記錄低嘅檔案，所以佢唔會刪除無關嘅檔案。如果你疊用咗幾種方法，就按呢個順序清理。第一，移除 Claude Code 外掛安裝。第二，喺 repo 根目錄執行 ECC 解除安裝，咁就會移除狀態管理嘅檔案。第三，刪除任何你手動複製過嘅額外 rule 資料夾。而第四，淨係用一條路線，重新安裝一次。一個安心嘅保證：如果你本機嘅 Claude 設定被清走咗，你唔需要再買過任何嘢。喺重新安裝之前，淨係執行 list-installed，跟住 doctor，再跟住 repair 就得。

講一講 MCP 伺服器：佢哋係可選擇加入嘅。外掛安裝刻意唔自動啟用 ECC 內附嘅 MCP 伺服器定義。咁可以喺嚴格嘅閘道上面避免過長嘅工具名，並且保護你嘅上下文視窗。ECC 剛好內附一個預設連接器，畀 Chrome 開發者工具用。其餘一切，要麼係一個包住命令列工具或者 REST API 嘅 skill，要麼就係一個可選擇加入嘅目錄項目。要加入 MCP 伺服器，你可以用 Claude Code 嘅 MCP command 做即時改動，又或者將你想要嘅伺服器複製入一個專案設定檔，畀 repo 本地存取。如果你已經行緊自己嗰份，你可以設定一個環境變數，咁安裝同同步流程就會跳過佢哋。而永遠記得將任何佔位金鑰換成真正嘅。上下文視窗嘅紀律，遲啲會有自己嘅一章。

仲有一個設定提示：多模型 commands 需要額外設定。Multi-plan、multi-execute、multi-backend、multi-frontend 同 multi-workflow 呢啲 commands，唔係基本安裝涵蓋嘅。佢哋要靠一個獨立嘅工作流程運行時，你用佢自己嘅 npx command 嚟安裝。嗰個提供咗呢啲 commands 預期嘅外部相依套件。冇咗佢，啲 multi commands 就唔會正確咁運行。

要驗證你嘅安裝，你可以喺 Claude Code 入面列出外掛提供咗啲乜、由 repo 執行測試套件，又或者影一張操作者就緒度快照，寫低一個可攜帶嘅交接檔案。當你執行 list-installed command 嗰陣，一個成功嘅手動安裝會顯示 ECC 管理嘅檔案。

最後，畀其他框架嘅幾個快速指引。完整嘅跨框架細節遲啲先講，但目標旗標係關鍵。你可以將目標設做 Cursor、用 minimal 設定檔將目標設做 Zed、執行一個同步腳本去合併入 Codex，又或者乾脆啟動 OpenCode，佢會自動偵測佢自己嘅設定目錄。

咁，重點精華。淨係一條路線：外掛或者手動安裝器，永遠唔好兩樣都用。外掛最快，但 rules 仍然需要手動複製，即係 common 嘅 rules 加一個語言包。設定檔有 minimal，冇 hooks；core；同 full，而 full 淨係手動嗰條路先用。如果你唔肯定要裝啲乜，用 consult command。用 list-installed、doctor 同 repair 嚟重設，跟住試行解除安裝，然後先重新安裝。而 MCP 伺服器同 multi commands 係刻意設成可選擇加入。

裝好咗 ECC，我哋就嚟學吓字彙：六大建構模塊。
